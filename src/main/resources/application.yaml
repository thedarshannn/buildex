spring:
  application:
    name: Buildex


  datasource:
    url: jdbc:postgresql://ep-nameless-butterfly-ama1fivu-pooler.c-5.us-east-1.aws.neon.tech/buildex?sslmode=require&channelBinding=require
    username: neondb_owner
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
